<div class="row">
  <div class="col-sm-6 col-md-4">
    <div class="thumbnail">
      <img src="{{ story.image }}" alt="{{ story.title }}">
      <div class="caption">
        <h3>{{ story.title }}</h3>
        <button {{#link-to 'story'}} <a href="#" class="btn btn-default" role="button">Button</a></p>
      </div>
    </div>
  </div>
</div>
